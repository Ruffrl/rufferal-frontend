import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface CreatePetPhotoProps {}

export const CreatePetPhoto = (props: CreatePetPhotoProps) => {
  return (
    <View>
      <Text>Welcome to create-pet-photo!</Text>
    </View>
  );
};
