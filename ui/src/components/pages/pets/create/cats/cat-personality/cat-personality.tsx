import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface CatPersonalityProps {}

export const CatPersonality = (props: CatPersonalityProps) => {
  return (
    <View>
      <Text>Welcome to cat - create personality!</Text>
    </View>
  );
};
