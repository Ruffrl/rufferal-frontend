import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface DogPersonalityProps {}

export const DogPersonality = (props: DogPersonalityProps) => {
  return (
    <View>
      <Text>Welcome to dog - create personality!</Text>
    </View>
  );
};
