import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface DogDetailsProps {}

export const DogDetails = (props: DogDetailsProps) => {
  return (
    <View>
      <Text>Welcome to dog - create details!</Text>
    </View>
  );
};
