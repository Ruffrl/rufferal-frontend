import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface DogAvatarProps {}

export const DogAvatar = (props: DogAvatarProps) => {
  return (
    <View>
      <Text>Welcome to dog - create avatar!</Text>
    </View>
  );
};
