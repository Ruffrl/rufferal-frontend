import { Text, View } from 'react-native';

/* eslint-disable-next-line */
export interface CreatePetPersonalityProps {}

export const CreatePetPersonality = (props: CreatePetPersonalityProps) => {
  return (
    <View>
      <Text>Welcome to create-pet-personality!</Text>
    </View>
  );
};
